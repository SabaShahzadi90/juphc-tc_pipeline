apiVersion: tekton.dev/v1beta1
kind: Pipeline
metadata:
  name: tax-calculator-pipeline
spec:
  tasks:
    - name: npm-install
      taskRef:
        name: npm-install
    - name: run-tests
      taskRef:
        name: run-jasmine-tests
      runAfter:
        - npm-install
    - name: build-image
      taskRef:
        name: build-and-push
      runAfter:
        - run-tests
